DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=pipeline_db
API_KEY=change-me-to-a-long-random-value

# Docker-specific (optional)
BACKEND_PORT=8080
FRONTEND_PORT=3000

# Frontend — must match API_KEY when using the UI to mutate pipelines
VITE_API_KEY=change-me-to-a-long-random-value
